## Vantech Software Solutions Admin

Run `npm run dev` inside `vantech-software-solutions-admin`.

The admin app reads shared deployment values from the root `.env` file and proxies backend requests through `/api`, keeping it in sync with the public frontend and backend configuration.
