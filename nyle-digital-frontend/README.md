## Nyle Digital Frontend

Run `npm run dev` inside `nyle-digital-frontend`.

The app reads shared deployment values from the root `.env` file and proxies backend requests through `/api`, so the frontend stays aligned with the same backend target as the admin app.
